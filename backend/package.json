{
  "name": "ademincol-backend",
  "version": "1.0.0",
  "description": "Backend de formularios (Cotizacion y PQRSF) para el sitio de Ademincol SA",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "nodemailer": "^6.9.16"
  }
}
